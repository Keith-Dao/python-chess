from enum import Enum

# Board dimensions
BOARD_WIDTH = BOARD_HEIGHT = 8

# Team colours
class Colours(Enum):
    WHITE: int = 0
    BLACK: int = 1

# Screen dimesions
SCREEN_WIDTH = SCREEN_HEIGHT = 700 #pixels
SQUARE_DIMENSIONS: int = 75 # pixels
UI_WIDTH = UI_HEIGHT = 50 # pixels
